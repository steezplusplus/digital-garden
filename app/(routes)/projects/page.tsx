import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Jesse's Homepage | Projects",
};

export default function ProjectsPage() {
  return (
    <div>
      <h1>Projects</h1>
    </div>
  );
}
